package bayes.enums;

/**
 * Enum representing way of counting category probability P(C), by number of word (docSom.pdf) 
 * or by number of examples (anywhere else)
 * @author Bartek
 * @version 1.0
 */
public enum CategoryProbabilityMode 
{
	BY_WORD,
	BY_EXAMPLES;
}
